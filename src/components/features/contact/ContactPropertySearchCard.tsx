import {
  Card,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";

export function ContactPropertySearchCard({ searchType }: { searchType: any }) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Property Search</CardTitle>
        <CardDescription></CardDescription>
      </CardHeader>
    </Card>
  );
}
